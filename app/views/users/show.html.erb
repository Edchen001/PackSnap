<div class="user_dash">
  <p><%= user.username %></p>
  <p><%= user.email %></p>
  <h3>Your Trips:</h3>
  <% user.trips.each do |trip| %>
  <p><%= link_to trip.title, trip_path(trip) %></p>
  <% end %>
</div>