<div class="container">
	<div class="dash-header">
		<h1 class="destination-title"><%= location.address %></h1>
		<h1 class="temp"><%= weather %>°F</h1>
		<br>
		<h3 class="summary"><%= summary %></h3>
	</div>
<div id="mi-slider" class="mi-slider">
	<ul>
	<% items.each do |item| %>
		<li><a href="#"><%= image_tag item.image.url(:thumb) %><h4><%= item.name %></h4></a></li>
		<% end %>
	</ul>
	<nav>
		<a href="#">PackSnap Suggestions</a>
	</nav>
</div>
	<div id="grid-gallery" class="grid-gallery">
		<%= render partial: "dash/grip_wrap", locals:{location: location} %>
		<%= render partial: "dash/slideshow", locals:{location: location} %>
	</div>
</div>
	<%= render partial: "dash/hidden_form", locals:{latitude: latitude, longitude: longitude, address: address} %>
