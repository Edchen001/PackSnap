<section class="grid-wrap">
  <ul class="grid">
    <li class="grid-sizer"></li>
    <% @location.comments.each do |comment| %>
    <li>
      <figure>
        <%= image_tag comment.item.image.url(:medium) %>
        <figcaption>
          <p><%= comment.item.name %></p>
          <p><%= comment.user.username %>,<%= @location.address %></p>
          <p><%= comment.content %></p>
        </figcaption>
      </figure>
    </li>
    <% end %>
  </ul>
</section>
